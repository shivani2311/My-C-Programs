# include<stdio.h>
# include<conio.h>
int main()
{
		char ch = '10';
		printf ("In character to ASCII conversation =>\n");
		printf("In Enter a character to Get it's ASCII value=");
		ch=getche();
		printf("/nASCII value of character %c is %d",ch,ch);
		printf ("/n/n thanks for using our service...");
getch();
return 0;
}
